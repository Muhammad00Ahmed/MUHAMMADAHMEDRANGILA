"use client";

// Note: This shadcn/ui form wrapper (built on react-hook-form) has been
// stubbed out. It is not used anywhere in the portfolio site, and the
// installed react-hook-form type definitions were broken, which blocked the
// production build. Restore the original shadcn form.tsx if you add forms.
export {};
